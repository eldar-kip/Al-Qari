<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/background_nav_bar"
    android:paddingBottom="5dp">

    <SeekBar
        android:id="@+id/audio_seekbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <LinearLayout
        android:id="@+id/down_audio_menu"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:baselineAligned="false"
        app:layout_constraintTop_toBottomOf="@id/audio_seekbar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Левая кнопка -->
        <LinearLayout
            android:id="@+id/audio_prev"
            style="@style/block_nav_bar"
            android:gravity="center">
            <ImageView
                android:layout_width="45dp"
                android:layout_height="45dp"
                android:scaleType="centerCrop"
                android:contentDescription="Previous"
                android:src="@drawable/media_pleir_reverse" />
        </LinearLayout>

        <!-- Центральная кнопка (Плей) -->
        <LinearLayout
            android:id="@+id/audio_play_pause"
            style="@style/block_nav_bar"
            android:gravity="center">
            <ImageView
                android:layout_width="45dp"
                android:layout_height="45dp"
                android:scaleType="centerCrop"
                android:contentDescription="Play/Pause"
                android:src="@drawable/media_playr_play" />
        </LinearLayout>

        <!-- Правая кнопка -->
        <LinearLayout
            android:id="@+id/audio_next"
            style="@style/block_nav_bar"
            android:gravity="center">
            <ImageView
                android:layout_width="45dp"
                android:layout_height="45dp"
                android:scaleType="centerCrop"
                android:contentDescription="Next"
                android:src="@drawable/media_playr_headphones" />
        </LinearLayout>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>